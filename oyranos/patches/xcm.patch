# use --libs because --cflags does not return something somehow.
Index: oyranos-0.4.0/configure_tests.sh
===================================================================
--- oyranos-0.4.0.orig/configure_tests.sh
+++ oyranos-0.4.0/configure_tests.sh
@@ -613,7 +613,7 @@ if [ -n "$XCM" ] && [ $XCM -gt 0 ]; then
     if [ -z "$found" ]; then
       pkg-config  --atleast-version=0.2 $pc_package
       if [ $? = 0 ]; then
-        found=`pkg-config --cflags $pc_package`
+        found=`pkg-config --libs $pc_package`
         version=`pkg-config --modversion $pc_package`
       fi
     fi
