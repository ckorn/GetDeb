#!/usr/bin/make -f
%:
	dh $@ --parallel

override_dh_auto_configure:
	dh_auto_configure -- -DQTONLY=ON

# The tests require a X server.
override_dh_auto_test:

override_dh_install:
	dh_install --list-missing
