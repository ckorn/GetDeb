### We want the output seen in the log. Not written to a file.
Index: avidemux2.6-2.6.15/bootStrap.bash
===================================================================
--- avidemux2.6-2.6.15.orig/bootStrap.bash	2016-11-19 12:51:26.161643482 +0100
+++ avidemux2.6-2.6.15/bootStrap.bash	2016-11-19 12:51:53.698089359 +0100
@@ -54,7 +54,7 @@
         fi
         cd $BUILDDIR 
         cmake $COMPILER $PKG $FAKEROOT $QT_FLAVOR -DCMAKE_EDIT_COMMAND=vim $INSTALL_PREFIX $EXTRA $ASAN $DEBUG -G "$BUILDER" $SOURCEDIR || fail cmakeZ
-        make  $PARAL >& /tmp/log$BUILDDIR || fail "make, result in /tmp/log$BUILDDIR"
+        make  $PARAL || fail "make"
 	if  [ "x$PKG" != "x" ] ; then
           $FAKEROOT_COMMAND make package DESTDIR=$FAKEROOT_DIR/tmp || fail package
 	fi
