#!/usr/bin/make -f
%:
	dh $@ --with autoreconf

override_dh_autoreconf:
	mkdir -p m4
	touch ChangeLog
	touch config.rpath
	dh_autoreconf

override_dh_auto_configure:
	# libgspell-1-dev not available in xenial
	dh_auto_configure -- --disable-spellcheck
