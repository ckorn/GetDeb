#!/usr/bin/make -f
%:
	dh $@ --with autoreconf

override_dh_autoreconf:
	mkdir -p m4
	touch ChangeLog
	touch config.rpath
	intltoolize --copy && aclocal
	AUTOPOINT="intltoolize --force --automake --copy" dh_autoreconf

