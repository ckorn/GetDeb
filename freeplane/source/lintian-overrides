# we cannot use the standard redirector because we need to limit the search
# on one sourceforge freeplane release directory ("freeplane stable")
freeplane source: debian-watch-file-should-use-sf-redirector
